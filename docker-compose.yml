version: "3.9"

services:
  apilocal:
    build: .
    ports:
      - "5000:5000"
    links:
      - db
  db:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
